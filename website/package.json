{
  "name": "slatedb-benchmark-website",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "scaled-data": "../scripts/fixtures.sh",
    "dev:scaled": "npm run scaled-data && BENCHMARK_RESULTS_ROOT=../.scaled-results/results astro dev",
    "build": "astro check && astro build",
    "build:scaled": "npm run scaled-data && BENCHMARK_RESULTS_ROOT=../.scaled-results/results npm run build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@fontsource/inter": "^5.2.6",
    "astro": "^7.0.9",
    "chart.js": "^4.4.9",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.3"
  }
}
