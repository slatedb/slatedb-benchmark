---
import type { BenchmarkResult, PricingTable } from '../lib/results';

interface Props {
  result: BenchmarkResult;
  pricing: PricingTable;
}

const { result, pricing } = Astro.props;
const hasWireRequestMetrics = result.storage.object_store_operations !== undefined;
const requests = hasWireRequestMetrics
  ? (result.storage.object_store_successful_requests ?? {})
  : {};
const basis = {
  elapsed_ns: result.elapsed_ns,
  database_size_bytes: result.storage.database_size_bytes,
};
const operationOrder = ['get', 'put', 'list', 'delete', 'head', 'copy', 'create_multipart', 'complete_multipart', 'abort_multipart'];
const requestEntries = Object.entries(requests)
  .filter(([, count]) => count > 0)
  .sort(([left], [right]) => operationOrder.indexOf(left) - operationOrder.indexOf(right));
const operationLabel = (operation: string) => operation.replaceAll('_', ' ');
---

<details
  class="detail-region cost-region section-disclosure"
  open
  data-cost-section
  data-pricing={JSON.stringify(pricing)}
  data-basis={JSON.stringify(basis)}
>
  <summary class="section-summary"><h2>Cost</h2></summary>

  <div class="section-body">
    <div class="cost-toolbar">
      <p>Projected 30-day object-storage bill based on this run.</p>
      <div class="cost-controls">
        <label>
          <span>Provider</span>
          <select data-cost-provider aria-label="Object store provider">
            {pricing.providers.map((provider) => <option value={provider.id}>{provider.name}</option>)}
          </select>
        </label>
      </div>
    </div>

    <div class="cost-statement">
      <header class="cost-statement-meta">
        <div><span>Service</span><strong data-cost-basis>—</strong></div>
        <div><span>Billing period</span><strong>{pricing.units.month_days} days</strong></div>
      </header>

      <section class="cost-line-group" aria-labelledby="request-charges-title">
        <div class="cost-line-heading">
          <h3 id="request-charges-title">Request charges</h3>
          <p>Successful HTTP request volume projected over the billing period</p>
        </div>
        {hasWireRequestMetrics ? (
          <div class="table-scroll">
            <table class="cost-line-table">
              <thead>
                <tr>
                  <th scope="col">Operation</th>
                  <th scope="col">Monthly requests</th>
                  <th scope="col">Rate / 1k</th>
                  <th scope="col">Amount</th>
                </tr>
              </thead>
              <tbody>
                {requestEntries.map(([operation, count]) => (
                  <tr data-cost-operation={operation} data-request-count={count}>
                    <th scope="row">{operationLabel(operation)}</th>
                    <td data-label="Monthly usage" data-cost-monthly-requests>—</td>
                    <td data-label="Rate / 1k" data-cost-operation-rate>—</td>
                    <td data-label="Amount" data-cost-operation-total>—</td>
                  </tr>
                ))}
              </tbody>
              <tfoot>
                <tr><th scope="row" colspan="3">Request subtotal</th><td data-cost-requests>—</td></tr>
              </tfoot>
            </table>
          </div>
        ) : (
          <p>Wire-level request metrics are unavailable for this historical result, so request charges are excluded.</p>
        )}
      </section>

      <section class="cost-line-group" aria-labelledby="storage-charges-title">
        <div class="cost-line-heading">
          <h3 id="storage-charges-title">Storage charges</h3>
          <p>Final database footprint retained for the billing period</p>
        </div>
        <div class="table-scroll">
          <table class="cost-line-table">
            <thead>
              <tr>
                <th scope="col">Item</th>
                <th scope="col">Stored</th>
                <th scope="col">Rate / GiB-month</th>
                <th scope="col">Amount</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <th scope="row">Database storage</th>
                <td data-label="Stored" data-cost-storage-usage>—</td>
                <td data-label="Rate / GiB-month" data-cost-storage-rate>—</td>
                <td data-label="Amount" data-cost-storage>—</td>
              </tr>
            </tbody>
            <tfoot>
              <tr><th scope="row" colspan="3">Storage subtotal</th><td data-cost-storage>—</td></tr>
            </tfoot>
          </table>
        </div>
      </section>

      <footer class="cost-grand-total">
        <div>
          <span>Estimated monthly total</span>
          <small>{pricing.currency} · {pricing.units.month_days}-day month</small>
        </div>
        <strong data-cost-total aria-live="polite">—</strong>
      </footer>
    </div>

    <div class="cost-context">
      <p>Request volume uses successful HTTP responses after client-side range coalescing, bulk deletion, pagination, and retries. Failed-attempt billing varies by provider and is excluded. Storage uses the final database size. Free tiers, credits, discounts, and taxes are excluded. Compute and storage are assumed to share a region.</p>
      <p class="cost-context-links">
        <a data-cost-source href={pricing.providers[0]?.source}>Provider pricing</a>
        <a href="/raw/prices.json">Raw price table</a>
      </p>
    </div>
  </div>
</details>

<script>
  type PricingProvider = {
    id: string;
    name: string;
    region: string;
    region_id: string;
    storage_class: string;
    source: string;
    storage_per_gib_month: number;
    requests_per_1000: Record<string, number>;
  };
  type PricingTable = {
    currency: string;
    units: { gib_bytes: number; month_days: number };
    providers: PricingProvider[];
  };
  type CostBasis = {
    elapsed_ns: number;
    database_size_bytes: number;
  };

  document.querySelectorAll<HTMLElement>('[data-cost-section]').forEach((section) => {
    const pricing = JSON.parse(section.dataset.pricing || '{}') as PricingTable;
    const basis = JSON.parse(section.dataset.basis || '{}') as CostBasis;
    const providerSelect = section.querySelector<HTMLSelectElement>('[data-cost-provider]');
    const totalOutput = section.querySelector<HTMLElement>('[data-cost-total]');
    const requestOutput = section.querySelector<HTMLElement>('[data-cost-requests]');
    const storageOutputs = [...section.querySelectorAll<HTMLElement>('[data-cost-storage]')];
    const storageUsageOutput = section.querySelector<HTMLElement>('[data-cost-storage-usage]');
    const storageRateOutput = section.querySelector<HTMLElement>('[data-cost-storage-rate]');
    const basisOutput = section.querySelector<HTMLElement>('[data-cost-basis]');
    const sourceLink = section.querySelector<HTMLAnchorElement>('[data-cost-source]');
    const operationRows = [...section.querySelectorAll<HTMLTableRowElement>('[data-cost-operation]')];

    const currency = (value: number) => {
      const digits = value === 0 ? 2 : value < 0.01 ? 6 : value < 1 ? 4 : 2;
      return new Intl.NumberFormat('en-US', {
        style: 'currency',
        currency: pricing.currency,
        minimumFractionDigits: digits,
        maximumFractionDigits: digits,
      }).format(value);
    };
    const rate = (value: number) => new Intl.NumberFormat('en-US', {
      style: 'currency',
      currency: pricing.currency,
      minimumFractionDigits: 3,
      maximumFractionDigits: 5,
    }).format(value);
    const requestCount = (value: number) => new Intl.NumberFormat('en-US', {
      maximumFractionDigits: 0,
    }).format(value);
    const storageQuantity = (value: number) => new Intl.NumberFormat('en-US', {
      minimumFractionDigits: 2,
      maximumFractionDigits: 2,
    }).format(value);

    const render = () => {
      const provider = pricing.providers.find((candidate) => candidate.id === providerSelect?.value)
        || pricing.providers[0];
      if (!provider) return;
      const elapsedSeconds = basis.elapsed_ns / 1e9;
      const monthSeconds = pricing.units.month_days * 86400;
      const requestScale = elapsedSeconds > 0 ? monthSeconds / elapsedSeconds : 0;
      const requestCost = operationRows.reduce((sum, row) => {
        const operation = row.dataset.costOperation || '';
        const monthlyRequests = Number(row.dataset.requestCount || 0) * requestScale;
        const operationRate = provider.requests_per_1000[operation] || 0;
        const operationCost = monthlyRequests / 1000 * operationRate;
        const monthlyRequestsOutput = row.querySelector<HTMLElement>('[data-cost-monthly-requests]');
        const rateOutput = row.querySelector<HTMLElement>('[data-cost-operation-rate]');
        const costOutput = row.querySelector<HTMLElement>('[data-cost-operation-total]');
        if (monthlyRequestsOutput) monthlyRequestsOutput.textContent = requestCount(monthlyRequests);
        if (rateOutput) rateOutput.textContent = rate(operationRate);
        if (costOutput) costOutput.textContent = currency(operationCost);
        return sum + operationCost;
      }, 0);
      const storageGib = basis.database_size_bytes / pricing.units.gib_bytes;
      const storageCost = storageGib * provider.storage_per_gib_month;

      if (totalOutput) totalOutput.textContent = currency(requestCost + storageCost);
      if (requestOutput) requestOutput.textContent = currency(requestCost);
      storageOutputs.forEach((output) => { output.textContent = currency(storageCost); });
      if (storageUsageOutput) storageUsageOutput.textContent = `${storageQuantity(storageGib)} GiB`;
      if (storageRateOutput) storageRateOutput.textContent = rate(provider.storage_per_gib_month);
      if (basisOutput) {
        basisOutput.textContent = `${provider.name} · ${provider.storage_class} · ${provider.region} (${provider.region_id})`;
      }
      if (sourceLink) sourceLink.href = provider.source;
    };

    providerSelect?.addEventListener('change', render);
    render();
  });
</script>
