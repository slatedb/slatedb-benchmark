# Generated by `slatedb-benchmark generate-workflow`. Do not edit.
name: Benchmark release

on:
  workflow_dispatch:
    inputs:
      slate_ref:
        description: SlateDB tag or commit
        required: true
        default: v0.14.1

permissions:
  contents: read

concurrency:
  group: benchmark-release
  cancel-in-progress: false

jobs:
  build-runner:
    runs-on: warp-ubuntu-latest-x64-16x
    outputs:
      slate_version: ${{ steps.selected-source.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: slatedb/slatedb
          ref: ${{ inputs.slate_ref }}
          path: .slatedb-source
      - uses: dtolnay/rust-toolchain@1.89.0
      - name: Configure selected SlateDB source
        run: scripts/select-slate-source.sh "$GITHUB_WORKSPACE/.slatedb-source"
      - name: Record selected source
        id: selected-source
        env:
          SLATE_REF: ${{ inputs.slate_ref }}
        run: |
          commit="$(git -C .slatedb-source rev-parse HEAD)"
          if [[ "$SLATE_REF" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            version="${SLATE_REF#v}"
          else
            version="sha-${commit:0:12}"
          fi
          echo "SLATEDB_COMMIT=$commit" >> "$GITHUB_ENV"
          echo "SLATEDB_VERSION=$version" >> "$GITHUB_ENV"
          echo "version=$version" >> "$GITHUB_OUTPUT"
      - name: Check selected SlateDB source
        run: cargo check --locked
      - name: Build runner
        run: cargo build --release --locked
      - name: Preserve runner binary
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-runner
          path: target/release/slatedb-benchmark
          overwrite: true
          retention-days: 7

  benchmark-rocksdb:
    name: Benchmark rocksdb
    needs: build-runner
    permissions:
      actions: write
      contents: write
    runs-on: warp-ubuntu-latest-x64-16x
    environment: benchmark
    env:
      CLOUD_PROVIDER: aws
      AWS_ACCESS_KEY_ID: ${{ secrets.TIGRIS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.TIGRIS_SECRET_ACCESS_KEY }}
      AWS_REGION: auto
      AWS_ENDPOINT_URL_S3: https://fly.storage.tigris.dev
      SLATEDB_BENCH_BUCKET: ${{ vars.TIGRIS_BUCKET_PREFIX }}-rocksdb
      SLATEDB_BENCH_PREFIX: release/${{ needs.build-runner.outputs.slate_version }}
      SLATEDB_BENCH_REGION: fra
      SLATEDB_BENCH_RUNNER_TYPE: warp-ubuntu-latest-x64-16x
    steps:
      - uses: actions/checkout@v4
      - name: Download runner binary
        uses: actions/download-artifact@v4
        with:
          name: benchmark-runner
          path: bin
      - name: Prepare runner binary
        run: chmod +x bin/slatedb-benchmark
      - name: Check out current main for publication
        uses: actions/checkout@v4
        with:
          ref: main
          path: publish
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: publish/website/package-lock.json
      - name: Run rocksdb / bulk-load
        run: |
          bin/slatedb-benchmark run \
            --suite "rocksdb" \
            --workload "bulk-load" \
            --session "github-${{ github.run_id }}-rocksdb" \
            --output ".runs/rocksdb"
          bin/slatedb-benchmark validate --output ".runs/rocksdb"
      - name: Publish rocksdb / bulk-load
        run: |
          ./scripts/publish-results.sh \
            ".runs/rocksdb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "rocksdb" \
            "bulk-load" \
            publish
      - name: Deploy rocksdb / bulk-load
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run rocksdb / random-read
        run: |
          bin/slatedb-benchmark run \
            --suite "rocksdb" \
            --workload "random-read" \
            --session "github-${{ github.run_id }}-rocksdb" \
            --output ".runs/rocksdb"
          bin/slatedb-benchmark validate --output ".runs/rocksdb"
      - name: Publish rocksdb / random-read
        run: |
          ./scripts/publish-results.sh \
            ".runs/rocksdb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "rocksdb" \
            "random-read" \
            publish
      - name: Deploy rocksdb / random-read
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run rocksdb / multi-random-read
        run: |
          bin/slatedb-benchmark run \
            --suite "rocksdb" \
            --workload "multi-random-read" \
            --session "github-${{ github.run_id }}-rocksdb" \
            --output ".runs/rocksdb"
          bin/slatedb-benchmark validate --output ".runs/rocksdb"
      - name: Publish rocksdb / multi-random-read
        run: |
          ./scripts/publish-results.sh \
            ".runs/rocksdb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "rocksdb" \
            "multi-random-read" \
            publish
      - name: Deploy rocksdb / multi-random-read
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run rocksdb / forward-range
        run: |
          bin/slatedb-benchmark run \
            --suite "rocksdb" \
            --workload "forward-range" \
            --session "github-${{ github.run_id }}-rocksdb" \
            --output ".runs/rocksdb"
          bin/slatedb-benchmark validate --output ".runs/rocksdb"
      - name: Publish rocksdb / forward-range
        run: |
          ./scripts/publish-results.sh \
            ".runs/rocksdb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "rocksdb" \
            "forward-range" \
            publish
      - name: Deploy rocksdb / forward-range
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run rocksdb / reverse-range
        run: |
          bin/slatedb-benchmark run \
            --suite "rocksdb" \
            --workload "reverse-range" \
            --session "github-${{ github.run_id }}-rocksdb" \
            --output ".runs/rocksdb"
          bin/slatedb-benchmark validate --output ".runs/rocksdb"
      - name: Publish rocksdb / reverse-range
        run: |
          ./scripts/publish-results.sh \
            ".runs/rocksdb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "rocksdb" \
            "reverse-range" \
            publish
      - name: Deploy rocksdb / reverse-range
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run rocksdb / overwrite
        run: |
          bin/slatedb-benchmark run \
            --suite "rocksdb" \
            --workload "overwrite" \
            --session "github-${{ github.run_id }}-rocksdb" \
            --output ".runs/rocksdb"
          bin/slatedb-benchmark validate --output ".runs/rocksdb"
      - name: Publish rocksdb / overwrite
        run: |
          ./scripts/publish-results.sh \
            ".runs/rocksdb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "rocksdb" \
            "overwrite" \
            publish
      - name: Deploy rocksdb / overwrite
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run rocksdb / read-while-writing
        run: |
          bin/slatedb-benchmark run \
            --suite "rocksdb" \
            --workload "read-while-writing" \
            --session "github-${{ github.run_id }}-rocksdb" \
            --output ".runs/rocksdb"
          bin/slatedb-benchmark validate --output ".runs/rocksdb"
      - name: Publish rocksdb / read-while-writing
        run: |
          ./scripts/publish-results.sh \
            ".runs/rocksdb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "rocksdb" \
            "read-while-writing" \
            publish
      - name: Deploy rocksdb / read-while-writing
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run rocksdb / forward-range-while-writing
        run: |
          bin/slatedb-benchmark run \
            --suite "rocksdb" \
            --workload "forward-range-while-writing" \
            --session "github-${{ github.run_id }}-rocksdb" \
            --output ".runs/rocksdb"
          bin/slatedb-benchmark validate --output ".runs/rocksdb"
      - name: Publish rocksdb / forward-range-while-writing
        run: |
          ./scripts/publish-results.sh \
            ".runs/rocksdb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "rocksdb" \
            "forward-range-while-writing" \
            publish
      - name: Deploy rocksdb / forward-range-while-writing
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run rocksdb / reverse-range-while-writing
        run: |
          bin/slatedb-benchmark run \
            --suite "rocksdb" \
            --workload "reverse-range-while-writing" \
            --session "github-${{ github.run_id }}-rocksdb" \
            --output ".runs/rocksdb"
          bin/slatedb-benchmark validate --output ".runs/rocksdb"
      - name: Publish rocksdb / reverse-range-while-writing
        run: |
          ./scripts/publish-results.sh \
            ".runs/rocksdb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "rocksdb" \
            "reverse-range-while-writing" \
            publish
      - name: Deploy rocksdb / reverse-range-while-writing
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Preserve suite artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-${{ needs.build-runner.outputs.slate_version }}-rocksdb
          path: .runs/rocksdb
          if-no-files-found: ignore
          overwrite: true

  benchmark-slatedb:
    name: Benchmark slatedb
    needs: build-runner
    permissions:
      actions: write
      contents: write
    runs-on: warp-ubuntu-latest-x64-16x
    environment: benchmark
    env:
      CLOUD_PROVIDER: aws
      AWS_ACCESS_KEY_ID: ${{ secrets.TIGRIS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.TIGRIS_SECRET_ACCESS_KEY }}
      AWS_REGION: auto
      AWS_ENDPOINT_URL_S3: https://fly.storage.tigris.dev
      SLATEDB_BENCH_BUCKET: ${{ vars.TIGRIS_BUCKET_PREFIX }}-slatedb
      SLATEDB_BENCH_PREFIX: release/${{ needs.build-runner.outputs.slate_version }}
      SLATEDB_BENCH_REGION: fra
      SLATEDB_BENCH_RUNNER_TYPE: warp-ubuntu-latest-x64-16x
    steps:
      - uses: actions/checkout@v4
      - name: Download runner binary
        uses: actions/download-artifact@v4
        with:
          name: benchmark-runner
          path: bin
      - name: Prepare runner binary
        run: chmod +x bin/slatedb-benchmark
      - name: Check out current main for publication
        uses: actions/checkout@v4
        with:
          ref: main
          path: publish
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: publish/website/package-lock.json
      - name: Run slatedb / cold-read
        run: |
          bin/slatedb-benchmark run \
            --suite "slatedb" \
            --workload "cold-read" \
            --session "github-${{ github.run_id }}-slatedb" \
            --output ".runs/slatedb"
          bin/slatedb-benchmark validate --output ".runs/slatedb"
      - name: Publish slatedb / cold-read
        run: |
          ./scripts/publish-results.sh \
            ".runs/slatedb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "slatedb" \
            "cold-read" \
            publish
      - name: Deploy slatedb / cold-read
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run slatedb / open-loop-read
        run: |
          bin/slatedb-benchmark run \
            --suite "slatedb" \
            --workload "open-loop-read" \
            --session "github-${{ github.run_id }}-slatedb" \
            --output ".runs/slatedb"
          bin/slatedb-benchmark validate --output ".runs/slatedb"
      - name: Publish slatedb / open-loop-read
        run: |
          ./scripts/publish-results.sh \
            ".runs/slatedb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "slatedb" \
            "open-loop-read" \
            publish
      - name: Deploy slatedb / open-loop-read
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run slatedb / open-loop-read-update
        run: |
          bin/slatedb-benchmark run \
            --suite "slatedb" \
            --workload "open-loop-read-update" \
            --session "github-${{ github.run_id }}-slatedb" \
            --output ".runs/slatedb"
          bin/slatedb-benchmark validate --output ".runs/slatedb"
      - name: Publish slatedb / open-loop-read-update
        run: |
          ./scripts/publish-results.sh \
            ".runs/slatedb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "slatedb" \
            "open-loop-read-update" \
            publish
      - name: Deploy slatedb / open-loop-read-update
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run slatedb / prefix-scan
        run: |
          bin/slatedb-benchmark run \
            --suite "slatedb" \
            --workload "prefix-scan" \
            --session "github-${{ github.run_id }}-slatedb" \
            --output ".runs/slatedb"
          bin/slatedb-benchmark validate --output ".runs/slatedb"
      - name: Publish slatedb / prefix-scan
        run: |
          ./scripts/publish-results.sh \
            ".runs/slatedb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "slatedb" \
            "prefix-scan" \
            publish
      - name: Deploy slatedb / prefix-scan
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run slatedb / sustained-ingest
        run: |
          bin/slatedb-benchmark run \
            --suite "slatedb" \
            --workload "sustained-ingest" \
            --session "github-${{ github.run_id }}-slatedb" \
            --output ".runs/slatedb"
          bin/slatedb-benchmark validate --output ".runs/slatedb"
      - name: Publish slatedb / sustained-ingest
        run: |
          ./scripts/publish-results.sh \
            ".runs/slatedb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "slatedb" \
            "sustained-ingest" \
            publish
      - name: Deploy slatedb / sustained-ingest
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run slatedb / transaction-contention
        run: |
          bin/slatedb-benchmark run \
            --suite "slatedb" \
            --workload "transaction-contention" \
            --session "github-${{ github.run_id }}-slatedb" \
            --output ".runs/slatedb"
          bin/slatedb-benchmark validate --output ".runs/slatedb"
      - name: Publish slatedb / transaction-contention
        run: |
          ./scripts/publish-results.sh \
            ".runs/slatedb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "slatedb" \
            "transaction-contention" \
            publish
      - name: Deploy slatedb / transaction-contention
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Preserve suite artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-${{ needs.build-runner.outputs.slate_version }}-slatedb
          path: .runs/slatedb
          if-no-files-found: ignore
          overwrite: true

  benchmark-ycsb:
    name: Benchmark ycsb
    needs: build-runner
    permissions:
      actions: write
      contents: write
    runs-on: warp-ubuntu-latest-x64-16x
    environment: benchmark
    env:
      CLOUD_PROVIDER: aws
      AWS_ACCESS_KEY_ID: ${{ secrets.TIGRIS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.TIGRIS_SECRET_ACCESS_KEY }}
      AWS_REGION: auto
      AWS_ENDPOINT_URL_S3: https://fly.storage.tigris.dev
      SLATEDB_BENCH_BUCKET: ${{ vars.TIGRIS_BUCKET_PREFIX }}-ycsb
      SLATEDB_BENCH_PREFIX: release/${{ needs.build-runner.outputs.slate_version }}
      SLATEDB_BENCH_REGION: fra
      SLATEDB_BENCH_RUNNER_TYPE: warp-ubuntu-latest-x64-16x
    steps:
      - uses: actions/checkout@v4
      - name: Download runner binary
        uses: actions/download-artifact@v4
        with:
          name: benchmark-runner
          path: bin
      - name: Prepare runner binary
        run: chmod +x bin/slatedb-benchmark
      - name: Check out current main for publication
        uses: actions/checkout@v4
        with:
          ref: main
          path: publish
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: publish/website/package-lock.json
      - name: Run ycsb / ycsb-a
        run: |
          bin/slatedb-benchmark run \
            --suite "ycsb" \
            --workload "ycsb-a" \
            --session "github-${{ github.run_id }}-ycsb" \
            --output ".runs/ycsb"
          bin/slatedb-benchmark validate --output ".runs/ycsb"
      - name: Publish ycsb / ycsb-a
        run: |
          ./scripts/publish-results.sh \
            ".runs/ycsb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "ycsb" \
            "ycsb-a" \
            publish
      - name: Deploy ycsb / ycsb-a
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run ycsb / ycsb-b
        run: |
          bin/slatedb-benchmark run \
            --suite "ycsb" \
            --workload "ycsb-b" \
            --session "github-${{ github.run_id }}-ycsb" \
            --output ".runs/ycsb"
          bin/slatedb-benchmark validate --output ".runs/ycsb"
      - name: Publish ycsb / ycsb-b
        run: |
          ./scripts/publish-results.sh \
            ".runs/ycsb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "ycsb" \
            "ycsb-b" \
            publish
      - name: Deploy ycsb / ycsb-b
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run ycsb / ycsb-c
        run: |
          bin/slatedb-benchmark run \
            --suite "ycsb" \
            --workload "ycsb-c" \
            --session "github-${{ github.run_id }}-ycsb" \
            --output ".runs/ycsb"
          bin/slatedb-benchmark validate --output ".runs/ycsb"
      - name: Publish ycsb / ycsb-c
        run: |
          ./scripts/publish-results.sh \
            ".runs/ycsb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "ycsb" \
            "ycsb-c" \
            publish
      - name: Deploy ycsb / ycsb-c
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run ycsb / ycsb-d
        run: |
          bin/slatedb-benchmark run \
            --suite "ycsb" \
            --workload "ycsb-d" \
            --session "github-${{ github.run_id }}-ycsb" \
            --output ".runs/ycsb"
          bin/slatedb-benchmark validate --output ".runs/ycsb"
      - name: Publish ycsb / ycsb-d
        run: |
          ./scripts/publish-results.sh \
            ".runs/ycsb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "ycsb" \
            "ycsb-d" \
            publish
      - name: Deploy ycsb / ycsb-d
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run ycsb / ycsb-e
        run: |
          bin/slatedb-benchmark run \
            --suite "ycsb" \
            --workload "ycsb-e" \
            --session "github-${{ github.run_id }}-ycsb" \
            --output ".runs/ycsb"
          bin/slatedb-benchmark validate --output ".runs/ycsb"
      - name: Publish ycsb / ycsb-e
        run: |
          ./scripts/publish-results.sh \
            ".runs/ycsb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "ycsb" \
            "ycsb-e" \
            publish
      - name: Deploy ycsb / ycsb-e
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Run ycsb / ycsb-f
        run: |
          bin/slatedb-benchmark run \
            --suite "ycsb" \
            --workload "ycsb-f" \
            --session "github-${{ github.run_id }}-ycsb" \
            --output ".runs/ycsb"
          bin/slatedb-benchmark validate --output ".runs/ycsb"
      - name: Publish ycsb / ycsb-f
        run: |
          ./scripts/publish-results.sh \
            ".runs/ycsb" \
            "${{ needs.build-runner.outputs.slate_version }}" \
            "ycsb" \
            "ycsb-f" \
            publish
      - name: Deploy ycsb / ycsb-f
        uses: actions/github-script@v9
        with:
          github-token: ${{ github.token }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: "pages.yml",
              ref: "main",
            });
      - name: Preserve suite artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-${{ needs.build-runner.outputs.slate_version }}-ycsb
          path: .runs/ycsb
          if-no-files-found: ignore
          overwrite: true
