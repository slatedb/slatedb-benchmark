diff --git a/slatedb/src/cached_object_store/storage_fs.rs b/slatedb/src/cached_object_store/storage_fs.rs
index 5ccb17d7..0aff586c 100644
--- a/slatedb/src/cached_object_store/storage_fs.rs
+++ b/slatedb/src/cached_object_store/storage_fs.rs
@@ -544,6 +544,7 @@ type FsCacheMutation = (std::path::PathBuf, EntryMutation);
 type FsCacheAccess = (std::path::PathBuf, usize);
 
 const EVICTOR_QUEUE_CAPACITY: usize = 100;
+const EVICTION_BATCH_SIZE_BYTES: usize = 64 * 1024 * 1024;
 // Minimum time between aggregated "evictor access queue is full" warnings.
 const QUEUE_FULL_LOG_INTERVAL_MS: i64 = 30_000;
 
@@ -664,8 +665,9 @@ impl FsCacheEvictor {
                     match mutation {
                         EntryMutation::Write(bytes) => {
                             inner
-                                .track_entry_accessed(path, bytes, system_clock.now(), true)
+                                .track_entry_accessed(path, bytes, system_clock.now())
                                 .await;
+                            inner.schedule_eviction();
                         }
                         EntryMutation::Delete => {
                             inner.delete_entry(path).await;
@@ -674,7 +676,7 @@ impl FsCacheEvictor {
                 }
                 Some((path, bytes)) = access_rx.recv() => {
                     inner
-                        .track_entry_accessed(path, bytes, system_clock.now(), false)
+                        .track_entry_accessed(path, bytes, system_clock.now())
                         .await;
                 }
                 else => return,
@@ -785,6 +787,7 @@ struct FsCacheEvictorInner {
     track_lock: Mutex<()>,
     cache_state: Mutex<CacheState>,
     cache_size_bytes: AtomicU64,
+    eviction_running: AtomicBool,
     stats: Arc<CachedObjectStoreStats>,
     rand: Arc<DbRand>,
     file_handle_cache: FileHandleCache,
@@ -804,6 +807,7 @@ impl FsCacheEvictorInner {
             track_lock: Mutex::new(()),
             cache_state: Mutex::new(CacheState::default()),
             cache_size_bytes: AtomicU64::new(0_u64),
+            eviction_running: AtomicBool::new(false),
             stats,
             rand,
             file_handle_cache,
@@ -849,20 +853,22 @@ impl FsCacheEvictorInner {
                 .into();
             let bytes = metadata.len() as usize;
 
-            self.track_entry_accessed(path, bytes, atime, evict).await;
+            self.track_entry_accessed(path, bytes, atime).await;
+        }
+
+        if evict {
+            self.schedule_eviction();
         }
     }
 
-    /// track the cache entry access, and evict the cache files when the cache size exceeds the limit if evict is true,
-    /// return the bytes of the evicted files. please note that track_entry_accessed might be called concurrently from
-    /// the rescanner and evictor tasks, it's expected to be wrapped with a lock to ensure the serial execution.
+    /// Track a cache entry access. This may be called concurrently by the
+    /// rescanner and access-consumer tasks, so updates are serialized.
     async fn track_entry_accessed(
         &self,
         path: std::path::PathBuf,
         bytes: usize,
         accessed_time: DateTime<Utc>,
-        evict: bool,
-    ) -> usize {
+    ) {
         let _track_guard = self.track_lock.lock().await;
 
         let entry_count = {
@@ -894,37 +900,64 @@ impl FsCacheEvictorInner {
         self.stats
             .object_store_cache_bytes
             .set(self.cache_size_bytes.load(Ordering::Relaxed) as i64);
+    }
 
-        // if the cache size is still below the limit, do nothing
+    /// Start one eviction worker if the cache is over its configured limit.
+    /// The caller never waits for file deletion, and the atomic flag keeps
+    /// concurrent writes and rescans from starting duplicate workers.
+    fn schedule_eviction(self: &Arc<Self>) {
         if self.cache_size_bytes.load(Ordering::Relaxed) <= self.max_cache_size_bytes as u64 {
-            return 0;
+            return;
         }
-        // TODO: check the disk space ratio here, if the disk space is low, also triggers evict.
-
-        // The maximum byte size the cache will take up on disk. If a write would cause the
-        // cache to exceed this threshold, entries are evicted using an 2-random strategy until
-        // the cache reaches 90% of `max_cache_size_bytes`.
-        //
-        // It's ok to call evict after inserting the new entry, because we will evict entries with eailer `accessed_time`.
-        // This ensures that the newly added entry will not be evicted immediately.
-        let evicted_bytes: usize = if evict
-            && self.cache_size_bytes.load(Ordering::Relaxed) > self.max_cache_size_bytes as u64
+
+        if self
+            .eviction_running
+            .compare_exchange(false, true, Ordering::AcqRel, Ordering::Acquire)
+            .is_err()
         {
-            // We sacrifice floating-point precision error to prevent possible overflow(i.e. self.max_cache_size_bytes * 9 / 10).
-            let target_size = ((self.max_cache_size_bytes as f64) * 0.9) as u64;
-            self.evict_to_target_size(target_size).await
-        } else {
-            0
-        };
+            return;
+        }
+
+        let inner = self.clone();
+        let _eviction_handle = tokio::spawn(async move {
+            inner.run_eviction().await;
+        });
+    }
+
+    async fn run_eviction(self: Arc<Self>) {
+        // We sacrifice floating-point precision to prevent possible overflow
+        // from multiplying max_cache_size_bytes by 9 before dividing by 10.
+        let target_size = ((self.max_cache_size_bytes as f64) * 0.9) as u64;
+        let mut stalled = false;
 
-        evicted_bytes
+        while self.cache_size_bytes.load(Ordering::Relaxed) > target_size {
+            let evicted_bytes = self
+                .evict_to_target_size(target_size, EVICTION_BATCH_SIZE_BYTES)
+                .await;
+            if evicted_bytes == 0 {
+                stalled = true;
+                break;
+            }
+
+            // Give foreground I/O and access tracking a chance to run between
+            // bounded deletion batches.
+            tokio::task::yield_now().await;
+        }
+
+        self.eviction_running.store(false, Ordering::Release);
+
+        // Close the race where a cache write observed an already-running
+        // worker just before that worker finished.
+        if !stalled
+            && self.cache_size_bytes.load(Ordering::Relaxed) > self.max_cache_size_bytes as u64
+        {
+            self.schedule_eviction();
+        }
     }
 
-    /// Evict cache entries until the cache size is below the target size.
-    /// This method acquires the lock once to pick all eviction targets, then releases the lock
-    /// to delete files, and finally acquires the lock again to update the state.
-    async fn evict_to_target_size(&self, target_size: u64) -> usize {
-        let picked_targets = self.pick_evict_targets(target_size).await;
+    /// Evict at most one bounded batch of cache entries toward the target size.
+    async fn evict_to_target_size(&self, target_size: u64, max_batch_bytes: usize) -> usize {
+        let picked_targets = self.pick_evict_targets(target_size, max_batch_bytes).await;
 
         if picked_targets.is_empty() {
             if self.cache_size_bytes.load(Ordering::Relaxed) > target_size {
@@ -1030,7 +1063,11 @@ impl FsCacheEvictorInner {
     //  of LRU, it randomized pick two files, compare their last access time, and choose the older one to evict.
     ///
     /// Returns a list of (path, size_bytes) tuples to evict.
-    async fn pick_evict_targets(&self, target_size: u64) -> Vec<(std::path::PathBuf, usize)> {
+    async fn pick_evict_targets(
+        &self,
+        target_size: u64,
+        max_batch_bytes: usize,
+    ) -> Vec<(std::path::PathBuf, usize)> {
         let cache_state = self.cache_state.lock().await;
 
         if cache_state.keys.len() < 2 {
@@ -1038,6 +1075,7 @@ impl FsCacheEvictorInner {
         }
 
         let mut targets = Vec::new();
+        let mut batch_bytes = 0;
         // Track the simulated cache size during eviction but do not modify the actual cache size until
         // after files are deleted.
         let mut simulated_size = self.cache_size_bytes.load(Ordering::Relaxed);
@@ -1092,8 +1130,15 @@ impl FsCacheEvictorInner {
                     (idx1, path1.clone(), entry1.size_bytes)
                 };
 
+            // Always allow one entry so an entry larger than the batch limit
+            // cannot permanently prevent eviction progress.
+            if !targets.is_empty() && batch_bytes.saturating_add(chosen_bytes) > max_batch_bytes {
+                break;
+            }
+
             picked_indices.insert(chosen_idx);
             simulated_size = simulated_size.saturating_sub(chosen_bytes as u64);
+            batch_bytes = batch_bytes.saturating_add(chosen_bytes);
             targets.push((chosen_path, chosen_bytes));
         }
 
@@ -1223,38 +1268,44 @@ mod tests {
     }
 
     #[tokio::test]
-    async fn test_evictor() {
+    async fn test_evictor_deletes_bounded_batches() {
         let temp_dir = tempfile::Builder::new()
             .prefix("objstore_cache_test_evictor_")
             .tempdir()
             .unwrap();
         let recorder = slatedb_common::metrics::MetricsRecorderHelper::noop();
 
-        let evictor = FsCacheEvictorInner::new(
+        let evictor = Arc::new(FsCacheEvictorInner::new(
             temp_dir.path().to_path_buf(),
             1024 * 2,
             Arc::new(CachedObjectStoreStats::new(&recorder)),
             Arc::new(DbRand::default()),
             FileHandleCache::new(1000),
-        );
+        ));
 
         let path0 = gen_rand_file(temp_dir.path(), "file0", 1024);
-        let evicted = evictor
-            .track_entry_accessed(path0, 1024, DefaultSystemClock::default().now(), true)
+        evictor
+            .track_entry_accessed(path0, 1024, DefaultSystemClock::default().now())
             .await;
-        assert_eq!(evicted, 0);
 
         let path1 = gen_rand_file(temp_dir.path(), "file1", 1024);
-        let evicted = evictor
-            .track_entry_accessed(path1, 1024, DefaultSystemClock::default().now(), true)
+        evictor
+            .track_entry_accessed(path1, 1024, DefaultSystemClock::default().now())
             .await;
-        assert_eq!(evicted, 0);
 
         let path2 = gen_rand_file(temp_dir.path(), "file2", 1024);
-        let evicted = evictor
-            .track_entry_accessed(path2, 1024, DefaultSystemClock::default().now(), true)
+        evictor
+            .track_entry_accessed(path2, 1024, DefaultSystemClock::default().now())
             .await;
-        assert_eq!(evicted, 2048);
+        assert_eq!(evictor.cache_size_bytes.load(Ordering::Relaxed), 3072);
+
+        let evicted = evictor.evict_to_target_size(1843, 1024).await;
+        assert_eq!(evicted, 1024);
+        assert_eq!(evictor.cache_size_bytes.load(Ordering::Relaxed), 2048);
+
+        let evicted = evictor.evict_to_target_size(1843, 1024).await;
+        assert_eq!(evicted, 1024);
+        assert_eq!(evictor.cache_size_bytes.load(Ordering::Relaxed), 1024);
 
         let file_paths = walkdir::WalkDir::new(temp_dir.path())
             .into_iter()
@@ -1263,6 +1314,41 @@ mod tests {
         assert_eq!(file_paths.len(), 2); // the folder file "." is also counted
     }
 
+    #[tokio::test]
+    async fn test_evictor_runs_eviction_in_background() {
+        let temp_dir = tempfile::Builder::new()
+            .prefix("objstore_cache_test_background_evictor_")
+            .tempdir()
+            .unwrap();
+        let recorder = slatedb_common::metrics::MetricsRecorderHelper::noop();
+        let evictor = Arc::new(FsCacheEvictorInner::new(
+            temp_dir.path().to_path_buf(),
+            1024 * 2,
+            Arc::new(CachedObjectStoreStats::new(&recorder)),
+            Arc::new(DbRand::default()),
+            FileHandleCache::new(1000),
+        ));
+
+        for idx in 0..3 {
+            let path = gen_rand_file(temp_dir.path(), &format!("file{idx}"), 1024);
+            evictor
+                .track_entry_accessed(path, 1024, DefaultSystemClock::default().now())
+                .await;
+        }
+
+        evictor.schedule_eviction();
+
+        for _ in 0..1000 {
+            if !evictor.eviction_running.load(Ordering::Acquire) {
+                break;
+            }
+            tokio::task::yield_now().await;
+        }
+
+        assert!(!evictor.eviction_running.load(Ordering::Acquire));
+        assert!(evictor.cache_size_bytes.load(Ordering::Relaxed) <= 1843);
+    }
+
     #[tokio::test]
     async fn test_evictor_separates_access_backpressure_from_cache_writes() {
         let temp_dir = tempfile::Builder::new()
@@ -1356,7 +1442,7 @@ mod tests {
 
         evictor.clone().scan_entries(false).await;
 
-        let targets = evictor.pick_evict_targets(1025).await;
+        let targets = evictor.pick_evict_targets(1025, usize::MAX).await;
 
         assert_eq!(targets.len(), 1);
         let (target_path, size) = &targets[0];
@@ -1482,12 +1568,12 @@ mod tests {
         // when: add two entries (within capacity)
         let path0 = gen_rand_file(temp_dir.path(), "file0", 1024);
         evictor
-            .track_entry_accessed(path0, 1024, DefaultSystemClock::default().now(), true)
+            .track_entry_accessed(path0, 1024, DefaultSystemClock::default().now())
             .await;
 
         let path1 = gen_rand_file(temp_dir.path(), "file1", 1024);
         evictor
-            .track_entry_accessed(path1, 1024, DefaultSystemClock::default().now(), true)
+            .track_entry_accessed(path1, 1024, DefaultSystemClock::default().now())
             .await;
 
         // then: cache_keys and cache_bytes are set
@@ -1497,8 +1583,9 @@ mod tests {
         // when: add a third entry that triggers eviction
         let path2 = gen_rand_file(temp_dir.path(), "file2", 1024);
         evictor
-            .track_entry_accessed(path2, 1024, DefaultSystemClock::default().now(), true)
+            .track_entry_accessed(path2, 1024, DefaultSystemClock::default().now())
             .await;
+        evictor.evict_to_target_size(1843, usize::MAX).await;
 
         // then: evicted_keys and evicted_bytes are recorded
         let evicted_keys = lookup_metric(&recorder, EVICTED_KEYS).unwrap();
