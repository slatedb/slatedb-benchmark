diff --git a/slatedb/src/compactor.rs b/slatedb/src/compactor.rs
index 604ac18ec515fa841d3a320535e463ac5789126f..02909e02f367eede505fdfd99462974673447bf0 100644
--- a/slatedb/src/compactor.rs
+++ b/slatedb/src/compactor.rs
@@ -405,6 +405,10 @@ impl Compactor {
             )
         });
         let worker_tx = worker.as_ref().map(|(_, tx, _)| tx.clone());
+        #[cfg(feature = "compaction_filters")]
+        let has_compaction_filter = self.compaction_filter_supplier.is_some();
+        #[cfg(not(feature = "compaction_filters"))]
+        let has_compaction_filter = false;
         let scheduler = Arc::from(self.scheduler_supplier.compaction_scheduler(&self.options));
         let handler = CompactorEventHandler::new(
             self.manifest_store.clone(),
@@ -417,6 +421,7 @@ impl Compactor {
             self.recorder.clone(),
         )
         .await?
+        .with_compaction_filter(has_compaction_filter)
         .with_worker_tx(worker_tx);
         self.task_executor
             .add_handler(
@@ -536,6 +541,9 @@ pub(crate) struct CompactorEventHandler {
     /// Direct notification path to an embedded worker. Standalone workers
     /// discover the same state changes through their poll ticker.
     worker_tx: Option<async_channel::Sender<WorkerMessage>>,
+    /// Compaction filters require every input entry to pass through the
+    /// executor, so they disable metadata-only trivial moves.
+    has_compaction_filter: bool,
 }
 
 #[async_trait]
@@ -620,9 +628,15 @@ impl CompactorEventHandler {
             prev_claimed: HashSet::new(),
             worker_heartbeat_gauges: HashMap::new(),
             worker_tx: None,
+            has_compaction_filter: false,
         })
     }
 
+    pub(crate) fn with_compaction_filter(mut self, has_compaction_filter: bool) -> Self {
+        self.has_compaction_filter = has_compaction_filter;
+        self
+    }
+
     pub(crate) fn with_worker_tx(
         mut self,
         worker_tx: Option<async_channel::Sender<WorkerMessage>>,
@@ -1222,14 +1236,52 @@ impl CompactorEventHandler {
         Ok(())
     }
 
-    /// Validates every `Submitted` compaction against the current manifest and
-    /// promotes the valid tiered specs to `Scheduled` — the coordinator's
-    /// "ready for a worker to claim" state. Drain specs short-circuit the
-    /// executor and are applied directly to the in-memory manifest (→
-    /// `Completed`). Invalid specs are marked `Failed`. State changes are
-    /// persisted before any worker (including the local executor) can act on
-    /// them: when any submission was a drain the manifest and `.compactions`
-    /// are written together, otherwise `.compactions` alone.
+    /// Builds a metadata-only output run when every input SST view has a
+    /// disjoint effective key range.
+    ///
+    /// The caller validates the compaction first, so every source resolves in
+    /// the target segment. Sorting preserves the output run invariant and makes
+    /// adjacent range checks sufficient to detect any overlap.
+    fn build_trivial_move_output(
+        compaction: &Compaction,
+        db_state: &ManifestCore,
+    ) -> Option<SortedRun> {
+        let destination = compaction.spec().destination()?;
+        let mut sst_views = compaction.get_l0_sst_views(db_state);
+        sst_views.extend(
+            compaction
+                .get_sorted_runs(db_state)
+                .into_iter()
+                .flat_map(|sr| sr.sst_views),
+        );
+        if sst_views.is_empty() {
+            return None;
+        }
+
+        sst_views.sort_by(|left, right| {
+            left.compacted_effective_start_key()
+                .cmp(right.compacted_effective_start_key())
+        });
+        let has_overlap = sst_views.windows(2).any(|pair| {
+            pair[0]
+                .compacted_effective_range()
+                .intersect(pair[1].compacted_effective_range())
+                .is_some()
+        });
+        if has_overlap {
+            return None;
+        }
+
+        Some(SortedRun {
+            id: destination,
+            sst_views,
+        })
+    }
+
+    /// Validates every `Submitted` compaction against the current manifest.
+    /// Drains and trivial moves update the manifest and transition directly to
+    /// `Completed`. Other valid tiered specs become `Scheduled`, ready for a
+    /// worker to claim. Invalid specs become `Failed`.
     ///
     /// Workers exclusively claim `Scheduled` entries; they never act on
     /// `Submitted`. Routing validation through this single chokepoint keeps
@@ -1247,7 +1299,7 @@ impl CompactorEventHandler {
             return Ok(());
         }
 
-        let any_drain = submitted_compactions.iter().any(|c| c.spec().is_drain());
+        let mut manifest_changed = false;
 
         for compaction in &submitted_compactions {
             // Validate the candidate compaction; mark as failed if invalid.
@@ -1262,21 +1314,42 @@ impl CompactorEventHandler {
                 continue;
             }
 
-            // Drain specs apply the watermark advance and SR removal directly,
-            // marking the compaction Completed. They never enter Scheduled
-            // because no worker runs them. Tiered specs become Scheduled so a
-            // worker can claim them.
             if compaction.spec().is_drain() {
                 self.state_mut().finish_drain_compaction(compaction.id());
-            } else {
-                self.state_mut().update_compaction(&compaction.id(), |c| {
-                    c.clear_ctx();
-                    c.set_status(CompactionStatus::Scheduled)
-                });
+                manifest_changed = true;
+                continue;
             }
+
+            if !self.has_compaction_filter {
+                if let Some(output) =
+                    Self::build_trivial_move_output(compaction, self.state().db_state())
+                {
+                    let estimated_bytes =
+                        Self::calculate_estimated_source_bytes(compaction, self.state().db_state());
+                    info!(
+                        "completed trivial move [id={}, source_views={}, bytes_reused={}]",
+                        compaction.id(),
+                        output.sst_views.len(),
+                        estimated_bytes
+                    );
+                    self.state_mut().finish_compaction(compaction.id(), output);
+                    self.stats.trivial_moves.increment(1);
+                    self.stats.trivial_move_bytes.increment(estimated_bytes);
+                    self.stats
+                        .last_compaction_ts
+                        .set(self.system_clock.now().timestamp());
+                    manifest_changed = true;
+                    continue;
+                }
+            }
+
+            self.state_mut().update_compaction(&compaction.id(), |c| {
+                c.clear_ctx();
+                c.set_status(CompactionStatus::Scheduled)
+            });
         }
 
-        if any_drain {
+        if manifest_changed {
             self.state_writer.write_state_safely().await?;
         } else {
             self.state_writer.write_compactions_safely().await?;
@@ -1358,6 +1431,8 @@ pub mod stats {
     pub const SSTS_WRITTEN: &str = compactor_stat_name!("ssts_written");
     pub const JOBS_CLAIMED: &str = compactor_stat_name!("jobs_claimed");
     pub const JOBS_RECLAIMED: &str = compactor_stat_name!("jobs_reclaimed");
+    pub const TRIVIAL_MOVES: &str = compactor_stat_name!("trivial_moves");
+    pub const TRIVIAL_MOVE_BYTES: &str = compactor_stat_name!("trivial_move_bytes");
     pub const WORKER_LAST_HEARTBEAT_MS: &str = compactor_stat_name!("worker_last_heartbeat_ms");
     /// Label key carrying a worker's id on per-worker metrics.
     pub const WORKER_ID_LABEL: &str = "worker_id";
@@ -1393,6 +1468,10 @@ pub mod stats {
         pub(crate) jobs_claimed: Arc<dyn CounterFn>,
         /// Stale jobs the coordinator reset `Running → Submitted`.
         pub(crate) jobs_reclaimed: Arc<dyn CounterFn>,
+        /// Tiered compactions completed without assigning work to a worker.
+        pub(crate) trivial_moves: Arc<dyn CounterFn>,
+        /// Estimated source bytes reused by metadata-only trivial moves.
+        pub(crate) trivial_move_bytes: Arc<dyn CounterFn>,
     }
 
     impl CompactionStats {
@@ -1402,6 +1481,8 @@ pub mod stats {
                 last_compaction_ts: recorder.gauge(LAST_COMPACTION_TS_SEC).register(),
                 jobs_claimed: recorder.counter(JOBS_CLAIMED).register(),
                 jobs_reclaimed: recorder.counter(JOBS_RECLAIMED).register(),
+                trivial_moves: recorder.counter(TRIVIAL_MOVES).register(),
+                trivial_move_bytes: recorder.counter(TRIVIAL_MOVE_BYTES).register(),
                 total_bytes_being_compacted: recorder.gauge(TOTAL_BYTES_BEING_COMPACTED).register(),
                 total_throughput: recorder.gauge(TOTAL_THROUGHPUT_BYTES_PER_SEC).register(),
                 merge_operator_compact_operands: recorder
@@ -1488,11 +1569,13 @@ mod tests {
     use ulid::Ulid;
 
     use super::*;
+    use crate::bytes_range::BytesRange;
     use crate::compaction_worker::WorkerMessage;
     use crate::compactions_store::{FenceableCompactions, StoredCompactions};
     use crate::compactor::stats::CompactionStats;
     use crate::compactor::stats::COMPACTOR_EPOCH;
     use crate::compactor::stats::LAST_COMPACTION_TS_SEC;
+    use crate::compactor::stats::{TRIVIAL_MOVES, TRIVIAL_MOVE_BYTES};
     use crate::compactor_executor::{
         CompactionExecutor, TokioCompactionExecutor, TokioCompactionExecutorOptions,
     };
@@ -3514,8 +3597,24 @@ mod tests {
 
         db.flush_with_options(flush_opts.clone()).await.unwrap();
 
-        // ticker time = 10, no expire time
+        // Overlap the first L0 so this test exercises rewriting rather than a
+        // trivial move.
         insert_clock.set(10);
+        db.put_with_options(
+            &[1; 16],
+            value,
+            &PutOptions {
+                ttl: Ttl::ExpireAt(10),
+            },
+            &WriteOptions {
+                await_durable: false,
+                ..Default::default()
+            },
+        )
+        .await
+        .unwrap();
+
+        // ticker time = 10, no expire time
         db.put_with_options(
             &[3; 16],
             value,
@@ -3560,7 +3659,7 @@ mod tests {
                 RowEntry::new_value(&[2; 16], value, 2)
                     .with_create_ts(5)
                     .with_expire_ts(i64::MAX),
-                RowEntry::new_value(&[3; 16], value, 3).with_create_ts(10),
+                RowEntry::new_value(&[3; 16], value, 4).with_create_ts(10),
             ],
         )
         .await;
@@ -3811,6 +3910,66 @@ mod tests {
         assert_eq!(actual, expected);
     }
 
+    #[test]
+    fn test_build_trivial_move_output_uses_projected_ranges() {
+        let left = test_sst_view(b"a", b"z").with_visible_range(BytesRange::from_ref("a".."m"));
+        let right = test_sst_view(b"a", b"z").with_visible_range(BytesRange::from_ref("m"..="z"));
+        let mut db_state = ManifestCore::new();
+        Arc::make_mut(&mut db_state.tree).l0 = VecDeque::from(vec![right.clone(), left.clone()]);
+        let compaction = Compaction::new(
+            Ulid::new(),
+            CompactionSpec::new(
+                vec![SourceId::SstView(right.id), SourceId::SstView(left.id)],
+                0,
+            ),
+        );
+
+        let output = CompactorEventHandler::build_trivial_move_output(&compaction, &db_state)
+            .expect("disjoint projections should be moved");
+
+        assert_eq!(output.id, 0);
+        assert_eq!(output.sst_views, vec![left, right]);
+    }
+
+    #[test]
+    fn test_build_trivial_move_output_rejects_shared_boundary() {
+        let left = test_sst_view(b"a", b"z").with_visible_range(BytesRange::from_ref("a"..="m"));
+        let right = test_sst_view(b"a", b"z").with_visible_range(BytesRange::from_ref("m"..="z"));
+        let mut db_state = ManifestCore::new();
+        Arc::make_mut(&mut db_state.tree).l0 = VecDeque::from(vec![right.clone(), left.clone()]);
+        let compaction = Compaction::new(
+            Ulid::new(),
+            CompactionSpec::new(
+                vec![SourceId::SstView(right.id), SourceId::SstView(left.id)],
+                0,
+            ),
+        );
+
+        assert!(CompactorEventHandler::build_trivial_move_output(&compaction, &db_state).is_none());
+    }
+
+    #[test]
+    fn test_build_trivial_move_output_combines_l0_and_sorted_run_views() {
+        let l0 = test_sst_view(b"m", b"z");
+        let sr_view = test_sst_view(b"a", b"f");
+        let mut db_state = ManifestCore::new();
+        let tree = Arc::make_mut(&mut db_state.tree);
+        tree.l0 = VecDeque::from(vec![l0.clone()]);
+        tree.compacted = vec![SortedRun {
+            id: 1,
+            sst_views: vec![sr_view.clone()],
+        }];
+        let compaction = Compaction::new(
+            Ulid::new(),
+            CompactionSpec::new(vec![SourceId::SstView(l0.id), SourceId::SortedRun(1)], 1),
+        );
+
+        let output = CompactorEventHandler::build_trivial_move_output(&compaction, &db_state)
+            .expect("disjoint L0 and sorted run views should be moved");
+
+        assert_eq!(output.sst_views, vec![sr_view, l0]);
+    }
+
     #[tokio::test]
     async fn test_should_track_per_job_throughput() {
         let start_time_ms = 1000u64;
@@ -4718,12 +4877,15 @@ mod tests {
 
         async fn write_l0(&mut self) {
             let mut rng = rng::new_test_rng(None);
+            let mut key = vec![0u8; self.options.l0_sst_size_bytes];
+            rng.fill_bytes(&mut key);
+            self.write_l0_with_key(&key).await;
+        }
+
+        async fn write_l0_with_key(&mut self, key: &[u8]) {
             let manifest = self.manifest.refresh().await.unwrap();
             let l0s = manifest.core.tree.l0.len();
-            // TODO: add an explicit flush_memtable fn to db and use that instead
-            let mut k = vec![0u8; self.options.l0_sst_size_bytes];
-            rng.fill_bytes(&mut k);
-            self.db.put(&k, &[b'x'; 10]).await.unwrap();
+            self.db.put(key, &[b'x'; 10]).await.unwrap();
             self.db.flush().await.unwrap();
             loop {
                 let manifest = self.manifest.refresh().await.unwrap().clone();
@@ -4733,6 +4895,12 @@ mod tests {
             }
         }
 
+        async fn write_overlapping_l0s(&mut self) {
+            let key = vec![b'k'; self.options.l0_sst_size_bytes];
+            self.write_l0_with_key(&key).await;
+            self.write_l0_with_key(&key).await;
+        }
+
         async fn build_l0_compaction(&mut self) -> CompactionSpec {
             let db_state = self.latest_db_state().await;
             let l0_ids_to_compact: Vec<SourceId> = db_state
@@ -4834,7 +5002,7 @@ mod tests {
     async fn test_should_record_last_compaction_ts() {
         // given:
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
-        fixture.write_l0().await;
+        fixture.write_overlapping_l0s().await;
         let compaction = fixture.build_l0_compaction().await;
         fixture.scheduler.inject_compaction(compaction.clone());
         fixture.handler.handle_ticker().await.unwrap();
@@ -4859,7 +5027,7 @@ mod tests {
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
         let (worker_tx, worker_rx) = async_channel::unbounded();
         fixture.handler.worker_tx = Some(worker_tx);
-        fixture.write_l0().await;
+        fixture.write_overlapping_l0s().await;
         let compaction = fixture.build_l0_compaction().await;
         fixture.scheduler.inject_compaction(compaction);
 
@@ -4878,7 +5046,7 @@ mod tests {
     #[tokio::test]
     async fn test_compactions_changed_commits_worker_result_without_ticker() {
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
-        fixture.write_l0().await;
+        fixture.write_overlapping_l0s().await;
         let compaction = fixture.build_l0_compaction().await;
         fixture.scheduler.inject_compaction(compaction);
         fixture.handler.handle_ticker().await.unwrap();
@@ -4899,7 +5067,7 @@ mod tests {
     async fn test_should_write_manifest_safely() {
         // given:
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
-        fixture.write_l0().await;
+        fixture.write_overlapping_l0s().await;
         let compaction = fixture.build_l0_compaction().await;
         fixture.scheduler.inject_compaction(compaction.clone());
         fixture.handler.handle_ticker().await.unwrap();
@@ -4946,7 +5114,7 @@ mod tests {
     async fn test_should_persist_compactions_on_start_and_finish() {
         // given:
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
-        fixture.write_l0().await;
+        fixture.write_overlapping_l0s().await;
         let compaction = fixture.build_l0_compaction().await;
         fixture.scheduler.inject_compaction(compaction.clone());
 
@@ -5036,11 +5204,20 @@ mod tests {
     }
 
     #[tokio::test]
-    async fn test_maybe_validate_submitted_compactions_promotes_to_scheduled() {
+    async fn test_maybe_validate_submitted_compactions_completes_trivial_move() {
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
         fixture.write_l0().await;
         fixture.handler.state_writer.refresh().await.unwrap();
         let spec = fixture.build_l0_compaction().await;
+        let source_view = fixture
+            .handler
+            .state()
+            .db_state()
+            .tree
+            .l0
+            .front()
+            .cloned()
+            .expect("missing source view");
         let compaction_id = Ulid::new();
         fixture
             .handler
@@ -5054,17 +5231,19 @@ mod tests {
             .await
             .unwrap();
 
-        // Scheduled is "ready to claim", not "running" — no local execution happens.
         let compactions = &fixture.handler.state_writer.state.compactions().value;
         assert_eq!(
             compactions
                 .get(&compaction_id)
                 .expect("missing compaction")
                 .status(),
-            CompactionStatus::Scheduled
+            CompactionStatus::Completed
         );
-        // Scheduled is persisted so remote workers (and a restarted coordinator)
-        // observe the same view.
+        let db_state = fixture.handler.state().db_state();
+        assert!(db_state.tree.l0.is_empty());
+        assert_eq!(db_state.tree.compacted.len(), 1);
+        assert_eq!(db_state.tree.compacted[0].sst_views, vec![source_view]);
+
         let stored_compactions = fixture
             .compactions_store
             .read_latest_compactions()
@@ -5076,10 +5255,126 @@ mod tests {
                 .get(&compaction_id)
                 .expect("missing stored compaction")
                 .status(),
+            CompactionStatus::Completed
+        );
+        assert_eq!(
+            slatedb_common::metrics::lookup_metric(&fixture.test_recorder, TRIVIAL_MOVES),
+            Some(1)
+        );
+        assert!(
+            slatedb_common::metrics::lookup_metric(&fixture.test_recorder, TRIVIAL_MOVE_BYTES)
+                .expect("missing trivial move byte metric")
+                > 0
+        );
+    }
+
+    #[tokio::test]
+    async fn test_maybe_validate_submitted_compactions_schedules_overlapping_inputs() {
+        let mut fixture = CompactorEventHandlerTestFixture::new().await;
+        fixture.write_overlapping_l0s().await;
+        fixture.handler.state_writer.refresh().await.unwrap();
+        let spec = fixture.build_l0_compaction().await;
+        let compaction_id = Ulid::new();
+        fixture
+            .handler
+            .state_mut()
+            .add_compaction(Compaction::new(compaction_id, spec))
+            .expect("failed to add compaction");
+
+        fixture
+            .handler
+            .maybe_validate_submitted_compactions()
+            .await
+            .unwrap();
+
+        assert_eq!(
+            fixture
+                .handler
+                .state()
+                .compactions()
+                .value
+                .get(&compaction_id)
+                .expect("missing compaction")
+                .status(),
+            CompactionStatus::Scheduled
+        );
+        assert_eq!(
+            slatedb_common::metrics::lookup_metric(&fixture.test_recorder, TRIVIAL_MOVES),
+            Some(0)
+        );
+    }
+
+    #[tokio::test]
+    async fn test_maybe_validate_submitted_compactions_filter_disables_trivial_move() {
+        let mut fixture = CompactorEventHandlerTestFixture::new().await;
+        fixture.write_l0().await;
+        fixture.handler.state_writer.refresh().await.unwrap();
+        fixture.handler.has_compaction_filter = true;
+        let spec = fixture.build_l0_compaction().await;
+        let compaction_id = Ulid::new();
+        fixture
+            .handler
+            .state_mut()
+            .add_compaction(Compaction::new(compaction_id, spec))
+            .expect("failed to add compaction");
+
+        fixture
+            .handler
+            .maybe_validate_submitted_compactions()
+            .await
+            .unwrap();
+
+        assert_eq!(
+            fixture
+                .handler
+                .state()
+                .compactions()
+                .value
+                .get(&compaction_id)
+                .expect("missing compaction")
+                .status(),
             CompactionStatus::Scheduled
         );
     }
 
+    #[cfg(feature = "compaction_filters")]
+    #[tokio::test]
+    async fn test_build_handler_propagates_compaction_filter() {
+        use crate::compaction_filter::{
+            CompactionFilter, CompactionFilterError, CompactionFilterSupplier, CompactionJobContext,
+        };
+
+        struct TestFilterSupplier;
+
+        #[async_trait]
+        impl CompactionFilterSupplier for TestFilterSupplier {
+            async fn create_compaction_filter(
+                &self,
+                _context: &CompactionJobContext,
+            ) -> Result<Box<dyn CompactionFilter>, CompactionFilterError> {
+                panic!("filter creation is not expected while building handlers")
+            }
+        }
+
+        let os: Arc<dyn ObjectStore> = Arc::new(InMemory::new());
+        let db = Db::builder(PATH, os.clone())
+            .with_settings(db_options(None))
+            .build()
+            .await
+            .unwrap();
+        let (manifest_store, compactions_store, table_store) = build_test_stores(os.clone());
+
+        let handlers = CompactorBuilder::new(PATH, os)
+            .with_options(compactor_options())
+            .with_compaction_filter_supplier(Arc::new(TestFilterSupplier))
+            .build_handler(table_store, manifest_store, compactions_store)
+            .await
+            .unwrap();
+
+        assert!(handlers.handler.has_compaction_filter);
+        db.close().await.unwrap();
+    }
+
     #[tokio::test]
     async fn test_maybe_validate_submitted_compactions_marks_invalid_failed() {
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
@@ -5188,7 +5483,7 @@ mod tests {
     }
 
     #[tokio::test]
-    async fn test_handle_ticker_starts_preexisting_submitted_compaction() {
+    async fn test_handle_ticker_completes_preexisting_trivial_move() {
         let compactor_options = Arc::new(compactor_options());
         let options = db_options(None);
 
@@ -5263,7 +5558,7 @@ mod tests {
 
         handler.handle_ticker().await.unwrap();
 
-        // The pre-existing Submitted compaction should be promoted to Scheduled.
+        // The coordinator reevaluates the persisted job and commits the move.
         let stored = compactions_store
             .read_latest_compactions()
             .await
@@ -5274,8 +5569,11 @@ mod tests {
                 .get(&compaction_id)
                 .expect("missing stored compaction")
                 .status(),
-            CompactionStatus::Scheduled
+            CompactionStatus::Completed
         );
+        let manifest = handler.state().db_state();
+        assert!(manifest.tree.l0.is_empty());
+        assert_eq!(manifest.tree.compacted.len(), 1);
     }
 
     #[tokio::test]
@@ -5310,7 +5608,7 @@ mod tests {
         // given: coordinator schedules a compaction and a worker completes it,
         // leaving a `Compacted` entry for the coordinator to commit.
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
-        fixture.write_l0().await;
+        fixture.write_overlapping_l0s().await;
         let compaction = fixture.build_l0_compaction().await;
         fixture.scheduler.inject_compaction(compaction.clone());
         fixture.handler.handle_ticker().await.unwrap();
@@ -5339,7 +5637,7 @@ mod tests {
     async fn test_should_not_schedule_conflicting_compaction() {
         // given: first ticker schedules a compaction (Scheduled in local state)
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
-        fixture.write_l0().await;
+        fixture.write_overlapping_l0s().await;
         let compaction = fixture.build_l0_compaction().await;
         fixture.scheduler.inject_compaction(compaction.clone());
         fixture.handler.handle_ticker().await.unwrap();
@@ -5358,7 +5656,7 @@ mod tests {
     async fn test_should_leave_checkpoint_when_removing_ssts_after_compaction() {
         // given:
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
-        fixture.write_l0().await;
+        fixture.write_overlapping_l0s().await;
         let compaction = fixture.build_l0_compaction().await;
         fixture.scheduler.inject_compaction(compaction.clone());
         fixture.handler.handle_ticker().await.unwrap();
@@ -5395,13 +5693,13 @@ mod tests {
         let os = Arc::new(InMemory::new());
         let system_clock = Arc::new(MockSystemClock::new());
         let scheduler_options = SizeTieredCompactionSchedulerOptions {
-            min_compaction_sources: 1,
+            min_compaction_sources: 2,
             max_compaction_sources: 999,
             include_size_threshold: 4.0,
         }
         .into();
         let mut options = db_options(Some(compactor_options()));
-        options.l0_sst_size_bytes = 128;
+        options.l0_sst_size_bytes = 4096;
         options.compression_codec = Some(CompressionCodec::Zstd);
         options
             .compactor_options
@@ -5419,17 +5717,18 @@ mod tests {
             .await
             .unwrap();
 
-        for i in 0..4 {
-            let k = vec![b'a' + i as u8; 16];
-            let v = vec![b'b' + i as u8; 48];
-            db.put(&k, &v).await.unwrap();
-            let k = vec![b'j' + i as u8; 16];
-            let v = vec![b'k' + i as u8; 48];
-            db.put(&k, &v).await.unwrap();
+        for generation in 0..2 {
+            for i in 0..4 {
+                let k = vec![b'a' + i as u8; 16];
+                let v = vec![b'b' + i as u8 + generation; 48];
+                db.put(&k, &v).await.unwrap();
+                let k = vec![b'j' + i as u8; 16];
+                let v = vec![b'k' + i as u8 + generation; 48];
+                db.put(&k, &v).await.unwrap();
+            }
+            db.flush().await.unwrap();
         }
 
-        db.flush().await.unwrap();
-
         // when:
         await_compaction(&db, os.clone(), Some(system_clock.clone()))
             .await
@@ -5681,6 +5980,7 @@ mod tests {
     #[tokio::test]
     async fn test_validate_compaction_rejects_parallel_l0() {
         let mut fixture = CompactorEventHandlerTestFixture::new().await;
+        fixture.handler.has_compaction_filter = true;
         // write two L0s so we can build two disjoint L0 compactions
         fixture.write_l0().await;
         fixture.write_l0().await;
@@ -6174,6 +6474,20 @@ mod tests {
 
     // Builds a fake output SsTableHandle with first_entry set so that
     // SsTableView::identity can derive a non-empty effective range.
+    fn test_sst_view(first_key: &[u8], last_key: &[u8]) -> SsTableView {
+        SsTableView::identity(SsTableHandle::new(
+            SsTableId::Compacted(Ulid::new()),
+            SST_FORMAT_VERSION_LATEST,
+            SsTableInfo {
+                first_entry: Some(Bytes::copy_from_slice(first_key)),
+                last_entry: Some(Bytes::copy_from_slice(last_key)),
+                index_offset: 100,
+                index_len: 20,
+                ..SsTableInfo::default()
+            },
+        ))
+    }
+
     fn fake_output_sst() -> SsTableHandle {
         SsTableHandle::new(
             SsTableId::Compacted(Ulid::new()),
diff --git a/slatedb/src/db.rs b/slatedb/src/db.rs
index e893c846e4268f160901d8457fc67817f10dc8a8..93219fe450f11b8628636cefbfbd8707988631f5 100644
--- a/slatedb/src/db.rs
+++ b/slatedb/src/db.rs
@@ -11509,8 +11509,7 @@ mod tests {
                 .await;
 
             for i in 0..2u32 {
-                let key = format!("key{:04}", i);
-                t.db().put(key.as_bytes(), &[b'v'; 64]).await.unwrap();
+                t.db().put(b"key", &[b'v' + i as u8; 64]).await.unwrap();
                 t.db()
                     .flush_with_options(FlushOptions {
                         flush_type: FlushType::MemTable,
@@ -11557,6 +11556,10 @@ mod tests {
 
             // Two ~10 MiB L0s; the ~20 MiB output crosses the multipart threshold.
             for sst in 0..2u32 {
+                if sst == 1 {
+                    // Overlap the first L0 so compaction rewrites both inputs.
+                    t.db().put(b"k0009", &vec![b'x'; MIB]).await.unwrap();
+                }
                 for i in 0..10u32 {
                     let key = format!("k{:04}", sst * 10 + i);
                     t.db()
@@ -11601,8 +11604,7 @@ mod tests {
             .await;
 
             for i in 0..2u32 {
-                let key = format!("key{:04}", i);
-                t.db().put(key.as_bytes(), &[b'v'; 64]).await.unwrap();
+                t.db().put(b"key", &[b'v' + i as u8; 64]).await.unwrap();
                 t.db()
                     .flush_with_options(FlushOptions {
                         flush_type: FlushType::MemTable,
@@ -11646,8 +11648,7 @@ mod tests {
                 let t = builder.build().await;
 
                 for i in 0..2u32 {
-                    let key = format!("key{:04}", i);
-                    t.db().put(key.as_bytes(), &[b'v'; 64]).await.unwrap();
+                    t.db().put(b"key", &[b'v' + i as u8; 64]).await.unwrap();
                     t.db()
                         .flush_with_options(FlushOptions {
                             flush_type: FlushType::MemTable,
diff --git a/slatedb/src/db/builder.rs b/slatedb/src/db/builder.rs
index c2daba5edb2512b224d857d5a6d1413e83e1beb8..7cd11f2e6a6d0faa7932d15a633b63d4b0744550 100644
--- a/slatedb/src/db/builder.rs
+++ b/slatedb/src/db/builder.rs
@@ -1317,6 +1317,10 @@ impl<P: Into<Path>> CompactorBuilder<P> {
         let scheduler_supplier = self
             .scheduler_supplier
             .unwrap_or(Arc::new(SizeTieredCompactionSchedulerSupplier));
+        #[cfg(feature = "compaction_filters")]
+        let has_compaction_filter = self.compaction_filter_supplier.is_some();
+        #[cfg(not(feature = "compaction_filters"))]
+        let has_compaction_filter = false;
         let (compactor_tx, rx) = async_channel::unbounded();
         let scheduler = Arc::from(scheduler_supplier.compaction_scheduler(&options));
         let stats = Arc::new(CompactionStats::new(&recorder));
@@ -1330,7 +1334,8 @@ impl<P: Into<Path>> CompactorBuilder<P> {
             self.system_clock.clone(),
             recorder.clone(),
         )
-        .await?;
+        .await?
+        .with_compaction_filter(has_compaction_filter);
         let worker = options.worker.clone().map(|worker_options| {
             CompactionWorkerHandler::build_worker_handler(
                 manifest_store,
