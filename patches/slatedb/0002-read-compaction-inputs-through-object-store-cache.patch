diff --git a/slatedb/src/cached_object_store/policy.rs b/slatedb/src/cached_object_store/policy.rs
--- a/slatedb/src/cached_object_store/policy.rs
+++ b/slatedb/src/cached_object_store/policy.rs
@@ -21,13 +21,11 @@ impl GetPolicy for DefaultGetPolicy {
             return GetAction::Bypass;
         };
 
-        // Compactor reads are one shot large scans that are not worth caching
-        // and GC doesn't issue any reads that would benefit from caching.
-        // Both bypass the cache. WAL reads are mainly during startup so they
-        // bypass the cache.
-        if matches!(tag.kind, TableStoreKind::Compactor | TableStoreKind::GC)
-            || tag.sst_type == SstType::Wal
-        {
+        // GC doesn't issue any reads that would benefit from caching. WAL reads
+        // are mainly during startup, so both bypass the cache. Compactor reads
+        // use the normal read-through path so repeated input reads can hit the
+        // object-store cache.
+        if tag.kind == TableStoreKind::GC || tag.sst_type == SstType::Wal {
             GetAction::Bypass
         } else if tag.retry.is_some() {
             // A reissued, non-bypassed read refetches.
@@ -89,7 +87,7 @@ impl GetPolicy for DefaultGetPolicy {
 pub(crate) enum GetAction {
     /// Skip the cache entirely: no lookup, nothing cached.
     ///
-    /// Used for things like compaction input scans and for WAL reads.
+    /// Used for things like GC reads and for WAL reads.
     Bypass,
     /// Drop any cached entry for the path, refetches from upstream and
     /// re-caches.
@@ -157,19 +155,7 @@ mod tests {
     }
 
     #[rstest]
-    // Compactor and GC reads bypass regardless of sst type or retry.
-    #[case(
-        Some(tag(TableStoreKind::Compactor, SstType::Compacted, None)),
-        GetAction::Bypass
-    )]
-    #[case(
-        Some(tag(
-            TableStoreKind::Compactor,
-            SstType::Compacted,
-            Some(RetryReason::CrcMismatch)
-        )),
-        GetAction::Bypass
-    )]
+    // GC reads bypass regardless of sst type or retry.
     #[case(
         Some(tag(TableStoreKind::GC, SstType::Compacted, None)),
         GetAction::Bypass
@@ -205,7 +191,19 @@ mod tests {
         )),
         GetAction::Refetch
     )]
-    // Main and reader reads take the normal read-through path.
+    #[case(
+        Some(tag(
+            TableStoreKind::Compactor,
+            SstType::Compacted,
+            Some(RetryReason::CrcMismatch)
+        )),
+        GetAction::Refetch
+    )]
+    // Main, reader, and compactor reads take the normal read-through path.
+    #[case(
+        Some(tag(TableStoreKind::Compactor, SstType::Compacted, None)),
+        GetAction::ReadThrough
+    )]
     #[case(
         Some(tag(TableStoreKind::Main, SstType::Compacted, None)),
         GetAction::ReadThrough
