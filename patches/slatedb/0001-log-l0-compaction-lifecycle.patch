diff --git a/slatedb/src/compaction_worker.rs b/slatedb/src/compaction_worker.rs
index 5cf86efd..8d576532 100644
--- a/slatedb/src/compaction_worker.rs
+++ b/slatedb/src/compaction_worker.rs
@@ -398,6 +398,23 @@ impl CompactionWorkerHandler {
                         self.worker_id,
                         compaction.id()
                     );
+                    let l0_view_ids = compaction
+                        .spec()
+                        .sources()
+                        .iter()
+                        .filter_map(|source| source.maybe_unwrap_sst_view())
+                        .collect::<Vec<_>>();
+                    if !l0_view_ids.is_empty() {
+                        info!(
+                            "l0_compaction_lifecycle event=started at_ms={} compaction_id={} trigger_l0_view_id={:?} l0_view_ids={:?} segment={:?} worker_id={}",
+                            self.clock.now().timestamp_millis(),
+                            compaction.id(),
+                            l0_view_ids.first(),
+                            l0_view_ids,
+                            compaction.spec().segment(),
+                            self.worker_id
+                        );
+                    }
                     self.job_progress
                         .insert(compaction.id(), compaction.ctx().cloned());
                     Self::dispatch_to_executor(&self.executor, args);
@@ -613,6 +630,14 @@ impl CompactionWorkerHandler {
                 );
                 return Ok(());
             }
+            let l0_view_ids = existing
+                .spec()
+                .sources()
+                .iter()
+                .filter_map(|source| source.maybe_unwrap_sst_view())
+                .collect::<Vec<_>>();
+            let segment = existing.spec().segment().clone();
+            let output_ssts = sorted_run.sst_views.len();
             let heartbeat_ms = self.clock.now().timestamp_millis() as u64;
             let updated = existing
                 .with_status(CompactionStatus::Compacted)
@@ -621,7 +646,21 @@ impl CompactionWorkerHandler {
                 .with_ctx(None);
             dirty.value.insert(updated);
             match stored.update(dirty).await {
-                Ok(()) => return Ok(()),
+                Ok(()) => {
+                    if !l0_view_ids.is_empty() {
+                        info!(
+                            "l0_compaction_lifecycle event=finished at_ms={} compaction_id={} trigger_l0_view_id={:?} l0_view_ids={:?} segment={:?} worker_id={} output_ssts={}",
+                            self.clock.now().timestamp_millis(),
+                            compaction_id,
+                            l0_view_ids.first(),
+                            l0_view_ids,
+                            segment,
+                            self.worker_id,
+                            output_ssts
+                        );
+                    }
+                    return Ok(());
+                }
                 Err(e) if e.is_sequenced_write_conflict() => continue,
                 Err(e) => return Err(e),
             }
diff --git a/slatedb/src/compactor.rs b/slatedb/src/compactor.rs
index c322496d..3b19f165 100644
--- a/slatedb/src/compactor.rs
+++ b/slatedb/src/compactor.rs
@@ -874,6 +874,7 @@ impl CompactorEventHandler {
         }
 
         let mut manifest_changed = false;
+        let mut committed_l0_compactions = Vec::new();
         for compaction in compacted {
             let id = compaction.id();
             match self.validate_compaction(&compaction) {
@@ -890,6 +891,19 @@ impl CompactorEventHandler {
                             .map(|sst| SsTableView::identity(sst.clone()))
                             .collect(),
                     };
+                    if compaction.spec().has_l0_sources() {
+                        let l0_view_ids = compaction
+                            .spec()
+                            .sources()
+                            .iter()
+                            .filter_map(SourceId::maybe_unwrap_sst_view)
+                            .collect::<Vec<_>>();
+                        committed_l0_compactions.push((
+                            id,
+                            compaction.spec().segment().clone(),
+                            l0_view_ids,
+                        ));
+                    }
                     self.state_mut().finish_compaction(id, output_sr);
                     manifest_changed = true;
                     self.stats
@@ -912,6 +926,17 @@ impl CompactorEventHandler {
         self.log_compaction_state();
         if manifest_changed {
             self.state_writer.write_state_safely().await?;
+            let at_ms = self.system_clock.now().timestamp_millis();
+            for (compaction_id, segment, l0_view_ids) in committed_l0_compactions {
+                info!(
+                    "l0_compaction_lifecycle event=manifest_committed at_ms={} compaction_id={} trigger_l0_view_id={:?} l0_view_ids={:?} segment={:?}",
+                    at_ms,
+                    compaction_id,
+                    l0_view_ids.first(),
+                    l0_view_ids,
+                    segment
+                );
+            }
         } else {
             // Validation failures only change `.compactions`. Avoid creating a
             // checkpoint and writing an unchanged manifest.
@@ -1201,6 +1226,7 @@ impl CompactorEventHandler {
         }
 
         let any_drain = submitted_compactions.iter().any(|c| c.spec().is_drain());
+        let mut scheduled_l0_compactions = Vec::new();
 
         for compaction in &submitted_compactions {
             // Validate the candidate compaction; mark as failed if invalid.
@@ -1222,6 +1248,19 @@ impl CompactorEventHandler {
             if compaction.spec().is_drain() {
                 self.state_mut().finish_drain_compaction(compaction.id());
             } else {
+                if compaction.spec().has_l0_sources() {
+                    let l0_view_ids = compaction
+                        .spec()
+                        .sources()
+                        .iter()
+                        .filter_map(SourceId::maybe_unwrap_sst_view)
+                        .collect::<Vec<_>>();
+                    scheduled_l0_compactions.push((
+                        compaction.id(),
+                        compaction.spec().segment().clone(),
+                        l0_view_ids,
+                    ));
+                }
                 self.state_mut().update_compaction(&compaction.id(), |c| {
                     c.clear_ctx();
                     c.set_status(CompactionStatus::Scheduled)
@@ -1234,6 +1273,17 @@ impl CompactorEventHandler {
         } else {
             self.state_writer.write_compactions_safely().await?;
         }
+        let at_ms = self.system_clock.now().timestamp_millis();
+        for (compaction_id, segment, l0_view_ids) in scheduled_l0_compactions {
+            info!(
+                "l0_compaction_lifecycle event=scheduled at_ms={} compaction_id={} trigger_l0_view_id={:?} l0_view_ids={:?} segment={:?}",
+                at_ms,
+                compaction_id,
+                l0_view_ids.first(),
+                l0_view_ids,
+                segment
+            );
+        }
 
         Ok(())
     }
diff --git a/slatedb/src/memtable_flusher/manifest_writer.rs b/slatedb/src/memtable_flusher/manifest_writer.rs
index 09e81414..338ff1a9 100644
--- a/slatedb/src/memtable_flusher/manifest_writer.rs
+++ b/slatedb/src/memtable_flusher/manifest_writer.rs
@@ -12,7 +12,7 @@
 //! - flush request semantics
 //! - flush waiter bookkeeping
 
-use log::debug;
+use log::{debug, info};
 
 use super::tracker::TrackerMessage;
 use super::uploader::UploadedMemtable;
@@ -47,6 +47,14 @@ pub(crate) struct FlushResult {
     pub(crate) durable_seq: u64,
 }
 
+struct WrittenL0 {
+    segment: Bytes,
+    l0_view_id: ulid::Ulid,
+    sst_id: SsTableId,
+    first_seq: u64,
+    last_seq: u64,
+}
+
 /// Command submitted to the manifest_writer.
 enum ManifestWriterCommand {
     /// One uploaded table is ready for ordered retirement.
@@ -440,7 +448,7 @@ impl ManifestWriterHandler {
         attached_checkpoints: Vec<PendingCheckpoint>,
         through_seq: u64,
     ) -> Result<(), SlateDBError> {
-        self.apply_uploaded_state(&staged_batch)?;
+        let written_l0s = self.apply_uploaded_state(&staged_batch)?;
 
         for uploaded in &staged_batch {
             uploaded.imm_memtable.notify_uploaded(Ok(()));
@@ -460,6 +468,18 @@ impl ManifestWriterHandler {
             .await
         {
             Ok(checkpoint_results) => {
+                let at_ms = self.db.system_clock.now().timestamp_millis();
+                for written in written_l0s {
+                    info!(
+                        "l0_compaction_lifecycle event=l0_written at_ms={} l0_view_id={} sst_id={:?} segment={:?} first_seq={} last_seq={}",
+                        at_ms,
+                        written.l0_view_id,
+                        written.sst_id,
+                        written.segment,
+                        written.first_seq,
+                        written.last_seq
+                    );
+                }
                 self.finish_ready_batch(
                     staged_batch,
                     attached_checkpoints,
@@ -476,7 +496,10 @@ impl ManifestWriterHandler {
         }
     }
 
-    fn apply_uploaded_state(&self, staged_batch: &[UploadedMemtable]) -> Result<(), SlateDBError> {
+    fn apply_uploaded_state(
+        &self,
+        staged_batch: &[UploadedMemtable],
+    ) -> Result<Vec<WrittenL0>, SlateDBError> {
         let min_active_snapshot_seq = [
             self.db.snapshot_manager.min_active_seq(),
             self.db.txn_manager.min_active_seq(),
@@ -485,6 +508,7 @@ impl ManifestWriterHandler {
         .flatten()
         .min();
         let segmented = self.db.segment_extractor.is_some();
+        let mut written_l0s = Vec::new();
         let mut guard = self.db.state.write();
         let manifest = guard.modify(|modifier| {
             for uploaded in staged_batch {
@@ -502,10 +526,8 @@ impl ManifestWriterHandler {
                 // still advances. (This is true with or without an
                 // extractor configured.)
                 for segment in &uploaded.segments {
-                    let view = SsTableView::new(
-                        self.db.rand.rng().gen_ulid(self.db.system_clock.as_ref()),
-                        segment.sst_handle.clone(),
-                    );
+                    let l0_view_id = self.db.rand.rng().gen_ulid(self.db.system_clock.as_ref());
+                    let view = SsTableView::new(l0_view_id, segment.sst_handle.clone());
                     let tree = if segmented {
                         // Extractor configured — every flush handle, including
                         // any with empty prefix, is routed into `segments`.
@@ -521,6 +543,13 @@ impl ManifestWriterHandler {
                         });
                     };
                     tree.l0.push_front(view);
+                    written_l0s.push(WrittenL0 {
+                        segment: segment.prefix.clone(),
+                        l0_view_id,
+                        sst_id: segment.sst_handle.id,
+                        first_seq: uploaded.first_seq,
+                        last_seq: uploaded.last_seq,
+                    });
                 }
                 core.replay_after_wal_id = uploaded.imm_memtable.recent_flushed_wal_id();
 
@@ -544,7 +573,7 @@ impl ManifestWriterHandler {
         })?;
 
         self.report_to_status_manager(&guard, manifest.into());
-        Ok(())
+        Ok(written_l0s)
     }
 
     /// Reports manifest and memtable updates to the status manager to notify subscriptions
@@ -659,16 +688,50 @@ impl ManifestWriterHandler {
         &self,
         remote_dirty: slatedb_txn_obj::DirtyObject<crate::manifest::Manifest>,
     ) {
-        let dirty_manifest = {
+        let (dirty_manifest, observed_compactions) = {
             let mut wguard_state = self.db.state.write();
+            let l0s_before = wguard_state
+                .state()
+                .core()
+                .trees_with_prefix()
+                .map(|(segment, tree)| {
+                    (
+                        segment,
+                        tree.l0.iter().map(|view| view.id).collect::<Vec<_>>(),
+                    )
+                })
+                .collect::<BTreeMap<_, _>>();
             wguard_state.merge_remote_manifest(remote_dirty);
             let cow = wguard_state.state();
             self.update_stats_for_manifest(&cow);
-            cow.manifest.clone()
+            let observed_compactions = l0s_before
+                .into_iter()
+                .filter_map(|(segment, previous_l0s)| {
+                    let tree = cow.core().tree_for_segment(&segment)?;
+                    let remaining_l0s = tree.l0.iter().map(|view| view.id).collect::<HashSet<_>>();
+                    let removed_l0s = previous_l0s
+                        .into_iter()
+                        .filter(|id| !remaining_l0s.contains(id))
+                        .collect::<Vec<_>>();
+                    (!removed_l0s.is_empty()).then_some((
+                        segment,
+                        tree.last_compacted_l0_sst_view_id,
+                        removed_l0s,
+                    ))
+                })
+                .collect::<Vec<_>>();
+            (cow.manifest.clone(), observed_compactions)
         };
         self.db
             .status_manager
             .report_manifest(dirty_manifest.into());
+        let at_ms = self.db.system_clock.now().timestamp_millis();
+        for (segment, trigger_l0_view_id, l0_view_ids) in observed_compactions {
+            info!(
+                "l0_compaction_lifecycle event=db_observed at_ms={} trigger_l0_view_id={:?} l0_view_ids={:?} segment={:?}",
+                at_ms, trigger_l0_view_id, l0_view_ids, segment
+            );
+        }
     }
 
     fn update_stats_for_manifest(&self, cow: &COWDbState) {
