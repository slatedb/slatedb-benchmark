diff --git a/slatedb/src/compactor_executor.rs b/slatedb/src/compactor_executor.rs
index 54f1ea9..d0754f6 100644
--- a/slatedb/src/compactor_executor.rs
+++ b/slatedb/src/compactor_executor.rs
@@ -3,6 +3,7 @@ use std::mem;
 use std::ops::{Bound, RangeBounds};
 use std::sync::atomic::{self, AtomicBool};
 use std::sync::Arc;
+use std::time::Instant;
 
 use bytes::Bytes;
 use futures::future::{join, join_all};
@@ -42,7 +43,7 @@ use crate::utils::{
     build_concurrent, compute_max_parallel, estimate_bytes_before_key, last_written_key_and_seq,
     spawn_bg_task, IdGenerator,
 };
-use log::{debug, error};
+use log::{debug, error, info};
 use tracing::instrument;
 use ulid::Ulid;
 
@@ -640,12 +641,14 @@ impl TokioCompactionExecutorInner {
 
         let (sub_tx, mut sub_rx) = tokio::sync::mpsc::unbounded_channel();
         let mut sub_tasks = Vec::new();
+        let mut started_at_by_sub = vec![Instant::now(); num_subcompactions];
         for args in subcompaction_args {
             // Each range runs as its own task, resuming from the output already
             // recorded against it. A range whose output is already complete
             // finds nothing left to merge and finishes immediately.
             let this = self.clone();
             let index = args.index;
+            started_at_by_sub[index] = Instant::now();
             let sequence_tracker = sequence_tracker.clone();
             let event_tx = sub_tx.clone();
             let finished_tx = sub_tx.clone();
@@ -711,6 +714,21 @@ impl TokioCompactionExecutorInner {
                 }
                 SubcompactionEvent::Finished { index, result } => match result {
                     Ok(output_ssts) => {
+                        let output_bytes = output_ssts
+                            .iter()
+                            .map(SsTableHandle::estimate_size)
+                            .sum::<u64>();
+                        info!(
+                            "subcompaction_lifecycle event=finished at_ms={} compaction_id={} subcompaction_index={} subcompactions={} elapsed_ms={} bytes_processed={} output_bytes={} output_ssts={}",
+                            self.clock.now().timestamp_millis(),
+                            id,
+                            index,
+                            num_subcompactions,
+                            started_at_by_sub[index].elapsed().as_millis(),
+                            bytes_processed_by_sub[index],
+                            output_bytes,
+                            output_ssts.len()
+                        );
                         ctx.set_output_ssts(index, output_ssts);
                         completed[index] = true;
                         let total_bytes = bytes_processed_by_sub.iter().sum();
@@ -878,5 +896,7 @@ impl TokioCompactionExecutorInner {
             output_ssts.push(sst);
         }
 
+        let total_bytes = start_bytes_processed + all_iter.bytes_processed();
+        progress(total_bytes, &output_ssts);
         Ok(output_ssts)
     }
